// Supabase configuration for pvcCRM.
// Load this file before CRM2.1.html if you want to keep credentials outside the page source.
window.PVC_CONFIG = {
  supabaseUrl: 'https://bnzsazqddvwejlhnmqbr.supabase.co',
  // Add the project's publishable/anon key here. Never use a service-role key in browser code.
  supabaseKey: window.PVC_SUPABASE_KEY || ''
};
